http {
    upstream myibalance {
        server srv1;
        server srv2;
        server srv3;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://mybalance;
        }
    }
}

